services:
  - type: web
    name: rive-lipsync-previewer
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.mjs
    envVars:
      - key: NODE_VERSION
        value: "20"
