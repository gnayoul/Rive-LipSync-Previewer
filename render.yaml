services:
  - type: web
    name: rive-lipsync-previewer
    runtime: node
    plan: free
    buildCommand: npm ci && npm --prefix web ci && npm run build:web
    startCommand: node server.mjs
    envVars:
      - key: NODE_VERSION
        value: "20"
